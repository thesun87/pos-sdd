{
  "name": "@pos-sdd/database",
  "version": "0.1.0",
  "private": true,
  "description": "Database package with Prisma client for pos-sdd monorepo",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc --noEmit",
    "lint": "eslint src --ext .ts",
    "type-check": "tsc --noEmit",
    "test": "vitest run --config vitest.config.ts",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@pos-sdd/config": "workspace:*",
    "typescript": "^6.0.0",
    "vitest": "^2.0.0"
  }
}
